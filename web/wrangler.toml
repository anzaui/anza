name = "anza-web"
compatibility_date = "2024-01-01"

# Cloudflare Pages will use these build settings
# when connecting via the dashboard:
#   Root directory: web
#   Build command: npm install && npm run build
#   Build output: dist
